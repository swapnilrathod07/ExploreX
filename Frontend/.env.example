VITE_API_URL=https://explorex-production-b8a0.up.railway.app
EXPLOREX_API_BASE=https://explorex-production-b8a0.up.railway.app

# Static HTML pages cannot read .env directly unless a build tool injects it.
# Frontend/config.js uses the same production backend URL at runtime.